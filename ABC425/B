#include <bits/stdc++.h>
using namespace std;

int main(){
  int N;
  vector<int> vec(N);
  for(int i=0;i<N;i++){
    cin >> vec.at(i);
  }
  
  vector<int> st(N);
  for(int i=0;i<N;i++){
    if (vec.at(i) != -1){
      st.at(i) = vec.at(i);
    }
  }
  cout << 
}
//解法が分かりませんでした
