#include <bits/stdc++.h>
using namespace std;

int main(){
  int n;
  cin >> n;
  vector<string> vec;
  for(int i=0;i<n;i++){
    string s;
    cin >> s;
    vec.push_back(s);
  }
  int x;
  string y;
  cin >> x >> y;
  if(vec.at(x-1) == y){
    cout << "Yes" << "\n";
  }
  else{
    cout << "No" << "\n";
  }
}
