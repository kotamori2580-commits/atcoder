#include <bits/stdc++.h>
using namespace std;

int main(){
  string S;
  int L;
  L = S.size();
  vector<charactor> vec(L);
  for(int i=0;i<L;i++){
    if(i != (L+1)/2-1){
      vec.push_back = S[i];     
    }
  }
  for (int i=0;i<L-1;i++){
    cout << vec.at(i) << endl;
  }
}
